# Kwik Delivery

## Features
This repository now contains multiple projects:

- `backend/` — Node/TypeScript backend API
- `restaurant/` — Flutter Restaurant Manager (admin) app
- `rider/` — Rider app (placeholder)

Quick commands from repository root (PowerShell):

`. .\scripts.ps1` + `DevBackend` or `DevAdmin` or `....`
check `scripts.ps1` for more .


Quick commands from repository root (bash):

```bash
    chmod +x scripts.sh
    source ./scripts.sh

    dev_backend
    dev_rider
    doctor

```